import 'package:flutter/material.dart';

/// KiloTap Color Palette
class AppColors {
  AppColors._();

  // Brand Colors
  static const Color sellerGreen = Color(0xFF1B8A5A);   // Household/Seller
  static const Color buyerBlue = Color(0xFF1A85C8);      // Collector/Buyer
  static const Color adminRed = Color(0xFFDC2626);       // Admin actions

  // Neutrals
  static const Color canvas = Color(0xFFF8F9FA);         // Background
  static const Color pureWhite = Color(0xFFFFFFFF);      // Cards, inputs
  static const Color inputGrey = Color(0xFFEDF1F3);      // Input fields, inactive
  static const Color divider = Color(0xFFD1D5DB);        // Borders

  // Text
  static const Color textPrimary = Color(0xFF111827);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textMuted = Color(0xFF9CA3AF);

  // Status
  static const Color success = Color(0xFF16A34A);
  static const Color warning = Color(0xFFD97706);
  static const Color error = Color(0xFFDC2626);
  static const Color star = Color(0xFFF59E0B);

  // UI
  static const Color shadow = Color(0x08000000);
}
